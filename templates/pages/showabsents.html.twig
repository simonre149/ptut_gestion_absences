{% extends 'base.html.twig' %}
{% block body %}
    <h1 style="text-align: center;">Absents</h1>
    <div class="col-md-4 mx-auto text-center">
        <table class="table">
            <thead>
            <tr>
                <th scope="col">Prénom</th>
                <th scope="col">Nom</th>
            </tr>
            </thead>
            {% for student in students %}
                <tr>
                    <td>{{ student.firstname }}</td>
                    <td> {{ student.name }}</td>
                </tr>
            {% endfor %}
        </table>
    </div>
{% endblock %}